# Staging validation (M1-T8)

Regenerated by `src/ingest/validate.py` on every `dvc repro`. Do not edit.

Checked: 13,567,319 events across 3,846 matches, 188,410 lineup rows.
Column checks: coordinate ranges (x 0–120, y 0–80, all start/end fields), period 1–5, xG in [0,1], required ids present.
Set checks: timestamps monotonic within (match, period); every event's match exists in matches; every acting player is on that match's team sheet.

## Result

**All green.** The staging layer is trusted from here on.
